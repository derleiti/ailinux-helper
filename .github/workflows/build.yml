name: AILinux Helper
on:
  workflow_dispatch:
  push:
    branches: [master]
    tags: ['v*']
    paths: ['apps/**', 'features/**', 'assets/**', 'release.json', 'scripts/generate-release-manifest.py', '.github/workflows/build.yml']

permissions:
  contents: write

jobs:
  prepare-release:
    name: Prepare Helper release
    runs-on: ubuntu-24.04
    steps:
      - name: Create tagged prerelease shell
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1 || \
            gh release create "$GITHUB_REF_NAME" \
              --repo "$GITHUB_REPOSITORY" \
              --verify-tag \
              --prerelease \
              --title "AILinux Helper ${GITHUB_REF_NAME#v}" \
              --notes "AILinux Helper ${GITHUB_REF_NAME#v}: global native vision/control, Android Accessibility input, typed desktop control, resilient workspace sharing, and isolated remote compute."
      - name: Branch build only
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: echo "No release upload for branch build."

  desktop:
    needs: prepare-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            script: dist:linux
            name: linux
            release_glob: '*.AppImage *.deb'
          - os: windows-2025
            script: dist:win
            name: windows
            release_glob: '*.exe'
          - os: macos-15
            script: dist:mac
            name: macos
            release_glob: '*.dmg *.zip'
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: apps/desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - run: npm ci
      - run: npm run check
      - run: npm run ${{ matrix.script }} -- --publish never
      - name: Upload desktop packages directly to tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          shopt -s nullglob
          files=( ${{ matrix.release_glob }} )
          ((${#files[@]} > 0)) || { echo "No ${{ matrix.name }} packages produced" >&2; exit 1; }
          checksum="SHA256SUMS-${{ matrix.name }}.txt"
          if command -v sha256sum >/dev/null; then sha256sum "${files[@]}" | sort -k2 > "$checksum"; else shasum -a 256 "${files[@]}" | sort -k2 > "$checksum"; fi
          gh release upload "$GITHUB_REF_NAME" "${files[@]}" "$checksum" --repo "$GITHUB_REPOSITORY" --clobber

  android:
    needs: prepare-release
    runs-on: ubuntu-24.04
    outputs:
      signing_available: ${{ steps.signing.outputs.available }}
    env:
      AILINUX_ANDROID_KEYSTORE_B64: ${{ secrets.AILINUX_ANDROID_KEYSTORE_B64 }}
      AILINUX_ANDROID_STORE_PASSWORD: ${{ secrets.AILINUX_ANDROID_STORE_PASSWORD }}
      AILINUX_ANDROID_KEY_ALIAS: ${{ secrets.AILINUX_ANDROID_KEY_ALIAS }}
      AILINUX_ANDROID_KEY_PASSWORD: ${{ secrets.AILINUX_ANDROID_KEY_PASSWORD }}
    defaults:
      run:
        working-directory: apps/android
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
      - name: Compile and test Android helper
        run: ./gradlew --no-daemon :app:testDebugUnitTest :app:assembleDebug
      - name: Detect production signing for tagged releases
        id: signing
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          if [[ -n "$AILINUX_ANDROID_KEYSTORE_B64" && -n "$AILINUX_ANDROID_STORE_PASSWORD" && -n "$AILINUX_ANDROID_KEY_PASSWORD" ]]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "Production Android signing is delegated to the trusted AILinux release host."
          fi
      - name: Configure Android release signing
        if: startsWith(github.ref, 'refs/tags/v') && steps.signing.outputs.available == 'true'
        shell: bash
        run: printf '%s' "$AILINUX_ANDROID_KEYSTORE_B64" | base64 --decode > "$RUNNER_TEMP/ailinux-release.jks"
      - name: Build signed Android release APK
        if: startsWith(github.ref, 'refs/tags/v') && steps.signing.outputs.available == 'true'
        env:
          AILINUX_ANDROID_KEYSTORE: ${{ runner.temp }}/ailinux-release.jks
        run: ./gradlew --no-daemon :app:assembleRelease
      - name: Verify and upload signed Android APK
        if: startsWith(github.ref, 'refs/tags/v') && steps.signing.outputs.available == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          AILINUX_ANDROID_KEYSTORE: ${{ runner.temp }}/ailinux-release.jks
        shell: bash
        run: |
          set -euo pipefail
          apk=app/build/outputs/apk/release/app-release.apk
          test -s "$apk"
          expected_alias="${AILINUX_ANDROID_KEY_ALIAS:-ailinux-workspace}"
          keytool -list -v -keystore "$AILINUX_ANDROID_KEYSTORE" -alias "$expected_alias" -storepass "$AILINUX_ANDROID_STORE_PASSWORD" | grep -E 'Alias name:|SHA256:'
          out="AILinux-Helper-${GITHUB_REF_NAME#v}-android.apk"
          cp "$apk" "$out"
          sha256sum "$out" > SHA256SUMS-android.txt
          gh release upload "$GITHUB_REF_NAME" "$out" SHA256SUMS-android.txt --repo "$GITHUB_REPOSITORY" --clobber

  release-check:
    name: Verify Helper release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [desktop, android]
    runs-on: ubuntu-24.04
    steps:
      - name: Verify required assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          assets=$(gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[].name')
          printf '%s\n' "$assets"
          version="${GITHUB_REF_NAME#v}"
          escaped_version="${version//./\\.}"
          for pattern in \
            "^AILinux-Helper-${escaped_version}-linux-.*\.AppImage$" \
            "^AILinux-Helper-${escaped_version}-linux-.*\.deb$" \
            "^AILinux-Helper-${escaped_version}-win-.*\.exe$" \
            "^AILinux-Helper-${escaped_version}-mac-.*\.dmg$" \
            '^SHA256SUMS-linux\.txt$' \
            '^SHA256SUMS-windows\.txt$' \
            '^SHA256SUMS-macos\.txt$'; do
            grep -Eq "$pattern" <<<"$assets" || { echo "missing release asset matching: $pattern" >&2; exit 1; }
          done
      - name: Verify CI-signed Android assets
        if: needs.android.outputs.signing_available == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          assets=$(gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[].name')
          version="${GITHUB_REF_NAME#v}"
          escaped_version="${version//./\\.}"
          grep -Eq "^AILinux-Helper-${escaped_version}-android\.apk$" <<<"$assets"
          grep -Eq '^SHA256SUMS-android\.txt$' <<<"$assets"
      - name: Mark fully CI-signed release ready
        if: needs.android.outputs.signing_available == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --prerelease=false --latest
      - name: Leave trusted-host Android release pending
        if: needs.android.outputs.signing_available != 'true'
        run: echo "Desktop release verified. Waiting for the trusted host to attach and verify the production-signed Android APK."
